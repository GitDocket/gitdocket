# Synthetic project guidance

This optional standalone bundle illustrates chosen standards and scoped procedures. These are example conventions, not requirements on GitDocket or an existing customer project. No spec, epic or task is required.

Read `docket/reference/project-guidance.md`, then open only sources relevant to the requested work. The deployment procedure is a harmless rehearsal and runs only when explicitly requested.
